<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myweb.www.repository.BoardDAO">
	
	<insert id="insertBoard" parameterType="bvo">
		insert into board(title, content, writer)
		values(#{title}, #{content}, #{writer})
	</insert>
	
	<select id="selectOneBoard" parameterType="long" resultType="bvo">
		select bno, title, content, writer, reg_at, mod_at, read_count
		from board where bno = #{bno}
	</select>
	
	<select id="selectListBoard" resultType="bvo">
		select bno, title, writer, reg_at, mod_at, read_count
		from board order by reg_at desc 
		<!-- where ${field} like #{query} -->
	</select>
	
	<update id="updateBoard" parameterType="bvo">
		update board set 
		title = #{title},
		content = #{content},
		mod_at = now() 
		where bno = #{bno}
	</update>
	
	 <update id="updateReadCntBoard" parameterType="long">
		update board set
		read_count = read_count + 1
		where bno = #{bno}
	</update> 

	<delete id="deleteBoard" parameterType="long">
		delete from board where bno = #{bno}
	</delete>
	
</mapper>